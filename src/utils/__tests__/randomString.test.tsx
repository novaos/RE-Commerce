import randomString from '../randomString';

// describe('[utils]', () => {
//   it('randomString should generate random string with length 10', () => {
//     expect(randomString(10).length).toBe(10);
//   });
// });

describe('[utils]', () => {
  it('randomString should generate random string with length 10', () => {
    expect(randomString(10).length).toBe(10);
  });
});
